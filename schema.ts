import { z } from "zod";

const defaultString = z.literal("--");

export const bodySchema = z
	.object({
		FilePath: z.string(),
		FolderPath: z.string(),
		FileName: z.string(),
		Title: z.string(),
		Title2: z.optional(z.string()),
		SDYYYY: z.string().length(4).or(defaultString),
		SDYY: z.string().length(2).or(defaultString),
		SDMM: z.string().length(2).or(defaultString),
		SDM: z.string().or(defaultString),
		SDDD: z.string().length(2).or(defaultString),
		SDD: z.string().or(defaultString),
		SDW: z.string().or(defaultString),
		STHH: z.string().length(2).or(defaultString),
		STH: z.string().or(defaultString),
		STMM: z.string().length(2).or(defaultString),
		STM: z.string().or(defaultString),
		STSS: z.string().length(2).or(defaultString),
		STS: z.string().or(defaultString),
		EDYYYY: z.string().length(4).or(defaultString),
		EDYY: z.string().length(2).or(defaultString),
		EDMM: z.string().length(2).or(defaultString),
		EDM: z.string().or(defaultString),
		EDDD: z.string().length(2).or(defaultString),
		EDD: z.string().or(defaultString),
		EDW: z.string().or(defaultString),
		ETHH: z.string().length(2).or(defaultString),
		ETH: z.string().or(defaultString),
		ETMM: z.string().length(2).or(defaultString),
		ETM: z.string().or(defaultString),
		ETSS: z.string().length(2).or(defaultString),
		ETS: z.string().or(defaultString),
		ONID10: z.string(),
		TSID10: z.string(),
		SID10: z.string(),
		EID10: z.string(),
		ONID16: z.string(),
		TSID16: z.string(),
		SID16: z.string(),
		EID16: z.string(),
		ServiceName: z.string(),
		SDYYYY28: z.string().length(4).or(defaultString),
		SDYY28: z.string().length(2).or(defaultString),
		SDMM28: z.string().length(2).or(defaultString),
		SDM28: z.string().or(defaultString),
		SDDD28: z.string().length(2).or(defaultString),
		SDD28: z.string().or(defaultString),
		SDW28: z.string().or(defaultString),
		STHH28: z.string().length(2).or(defaultString),
		STH28: z.string().or(defaultString),
		EDYYYY28: z.string().length(4).or(defaultString),
		EDYY28: z.string().length(2).or(defaultString),
		EDMM28: z.string().length(2).or(defaultString),
		EDM28: z.string().or(defaultString),
		EDDD28: z.string().length(2).or(defaultString),
		EDD28: z.string().or(defaultString),
		EDW28: z.string().or(defaultString),
		ETHH28: z.string().length(2).or(defaultString),
		ETH28: z.string().or(defaultString),
		DUHH: z.string().length(2).or(defaultString),
		DUH: z.string().or(defaultString),
		DUMM: z.string().length(2).or(defaultString),
		DUM: z.string().or(defaultString),
		DUSS: z.string().length(2).or(defaultString),
		DUS: z.string().or(defaultString),
		Drops: z.string(),
		Scrambles: z.string(),
		Result: z.string(),
		TitleF: z.string(),
		Title2F: z.string(),
		Genre: z.string(),
		Genre2: z.string(),
		AddKey: z.string(),
		SubTitle: z.string(),
		SubTitle2: z.string(),
		BatFileTag: z.string(),
		RecInfoID: z.string(),
		ReserveID: z.string(),
		RecMode: z.string(),
		ReserveComment: z.string(),
		NotifyID: z.string(),
		NotifyName: z.string(),
		ServiceNameHashTag: z.string().startsWith("#"),
		TitleHashTag: z.string(),
		ServiceNameHankaku: z.string(),
		TitleHankaku: z.string(),
		Title2Hankaku: z.string(),
		TimeYYYY: z.string().length(4),
		TimeYY: z.string().length(2),
		TimeMM: z.string().length(2),
		TimeM: z.string(),
		TimeDD: z.string().length(2),
		TimeD: z.string(),
		TimeW: z.string(),
		TimeHH: z.string().length(2),
		TimeH: z.string(),
		TimeII: z.string().length(2),
		TimeI: z.string(),
		TimeSS: z.string().length(2),
		TimeS: z.string(),
	})
	.partial({
		SDYYYY28: true,
		SDYY28: true,
		SDMM28: true,
		SDM28: true,
		SDDD28: true,
		SDD28: true,
		SDW28: true,
		STHH28: true,
		STH28: true,
		EDYYYY28: true,
		EDYY28: true,
		EDMM28: true,
		EDM28: true,
		EDDD28: true,
		EDD28: true,
		EDW28: true,
		ETHH28: true,
		ETH28: true,
		Drops: true,
		Scrambles: true,
		Result: true,
		TitleF: true,
		Title2F: true,
		Genre: true,
		Genre2: true,
		AddKey: true,
		SubTitle: true,
		SubTitle2: true,
		RecInfoID: true,
		NotifyID: true,
		NotifyName: true,
	});
